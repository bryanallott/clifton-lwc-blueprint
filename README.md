# LWC Training Phases - Jekyll site

This workspace has been scaffolded to serve the existing static page via Jekyll.

Build & serve locally:

```bash
gem install jekyll bundler
bundle exec jekyll serve
```

The generated site will appear in `_site/` by default. The root `index.html` contains Jekyll front matter and will be processed by Jekyll.
